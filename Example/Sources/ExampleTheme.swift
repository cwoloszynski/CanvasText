//
//  ExampleTheme.swift
//  Example
//
//  Created by Charlie Woloszynski on 7/22/17.
//  Copyright © 2017 Canvas Labs, Inc. All rights reserved.
//

import Foundation
import CanvasText
import UIKit

class ExampleTheme: Theme {
	/// Comment background color
	var commentBackgroundColor = UIColor.orange

	/// Code span background color
	var codeSpanBackgroundColor = UIColor.darkGray

	/// Code span text color
	var codeSpanColor = UIColor.purple

	/// Link URL color
	var linkURLColor = UIColor.blue

	/// Strikethrough color
	var strikethroughColor = UIColor.red

	/// Color of folded markdown characters
	var foldedColor = UIColor.darkGray

	/// Image placeholder background color
	var imagePlaceholderBackgroundColor = UIColor.lightGray

	/// Image placeholder icon color
	var imagePlaceholderColor = UIColor.lightText

	/// Horizontal rule fill color
	var horizontalRuleColor = UIColor.lightGray

	/// Heading level six color
	var headingSixColor = UIColor.darkGray

	/// Heading level five color
	var headingFiveColor = UIColor.purple

	/// Heading level four color
	var headingFourColor = UIColor.blue

	/// Heading level one color
	var headingOneColor = UIColor.red

	/// Heading level two color
	var headingTwoColor = UIColor.orange

	/// Heading level three color
	var headingThreeColor = UIColor.yellow

	/// Blockquote border color
	var blockquoteBorderColor = UIColor.gray

	/// Blockquote text color
	var blockquoteColor = UIColor.lightGray

	/// Code block line number background color
	var codeBlockLineNumberBackgroundColor = UIColor.darkGray

	/// Code block line number text color
	var codeBlockLineNumberColor = UIColor.cyan

	/// Code block background color
	var codeBlockBackgroundColor = UIColor.black

	/// Code block and code span text color
	var codeColor = UIColor.orange

	/// Unordered list item number color
	var orderedListItemNumberColor = UIColor.darkGray

	/// Unchecked checkbox border color
	var uncheckedCheckboxColor = UIColor.green

	/// Unordered list item bullet fill color
	var bulletColor = UIColor.blue

	/// Title placeholder color
	var titlePlaceholderColor =  UIColor.lightGray
	
	/// Tint color
	var tintColor = UIColor.yellow

	/// Primary text color
	var foregroundColor = UIColor.white

	/// Editor background color
	var backgroundColor = UIColor.black

	
}
